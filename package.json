{
  "name": "portfolio-live",
  "private": true,
  "description": "Static portfolio (GitHub Pages) + Vercel serverless API for lab tools",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "vercel dev",
    "test": "node --test api/bro-taxonomy.test.js",
    "test:bro-regression": "node scripts/bro-regression.mjs"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  }
}
